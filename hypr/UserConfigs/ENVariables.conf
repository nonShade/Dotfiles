# /* ---- 💫 https://github.com/JaKooLit 💫 ---- */  #
# Environment Variables Configuration
# See https://wiki.hyprland.org/Configuring/Environment-variables/

# =============================================================================
# WAYLAND TOOLKIT BACKEND VARIABLES
# =============================================================================
env = GDK_BACKEND,wayland,x11,*
env = QT_QPA_PLATFORM,wayland;xcb
env = CLUTTER_BACKEND,wayland

# =============================================================================
# SDL2 WAYLAND SUPPORT
# =============================================================================
# Run SDL2 applications on Wayland
# Remove or set to x11 if games cause compatibility issues
# env = SDL_VIDEODRIVER,wayland

# =============================================================================
# XDG DESKTOP SPECIFICATIONS
# =============================================================================
env = XDG_CURRENT_DESKTOP,Hyprland
env = XDG_SESSION_DESKTOP,Hyprland
env = XDG_SESSION_TYPE,wayland

# =============================================================================
# QT CONFIGURATION
# =============================================================================
env = QT_AUTO_SCREEN_SCALE_FACTOR,1
env = QT_WAYLAND_DISABLE_WINDOWDECORATION,1
env = QT_QPA_PLATFORMTHEME,qt5ct
env = QT_QPA_PLATFORMTHEME,qt6ct

# Hyprland Qt support
env = QT_QUICK_CONTROLS_STYLE,org.hyprland.style

# =============================================================================
# APPLICATION SCALING
# =============================================================================
# XWayland apps scale fix (useful if using monitor scaling)
# Set same value if you use scaling in monitors.conf
# 1 = 100%, 1.5 = 150%, etc.
# See https://wiki.hyprland.org/Configuring/XWayland/
env = GDK_SCALE,1
env = QT_SCALE_FACTOR,1

# =============================================================================
# CURSOR CONFIGURATION
# =============================================================================
# Bibata Modern Ice Cursor
# NOTE: You must have the hyprcursor version to activate this
# https://wiki.hyprland.org/Hypr-Ecosystem/hyprcursor/
env = HYPRCURSOR_THEME,Bibata-Modern-Ice
env = HYPRCURSOR_SIZE,24

# =============================================================================
# FIREFOX WAYLAND SUPPORT
# =============================================================================
env = MOZ_ENABLE_WAYLAND,1

# =============================================================================
# ELECTRON APPLICATIONS OPTIMIZATION
# =============================================================================
# For Electron >28 apps (may help)
# https://www.electronjs.org/docs/latest/api/environment-variables
env = ELECTRON_OZONE_PLATFORM_HINT,auto # auto selects Wayland if possible, X11 otherwise

# =============================================================================
# AQUAMARINE ENVIRONMENT VARIABLES (Hyprland > 0.45)
# =============================================================================
# https://wiki.hyprland.org/Configuring/Environment-variables/#aquamarine-environment-variables
# Uncomment if needed for debugging or specific GPU configurations:
# env = AQ_TRACE,1 # Enables more verbose logging
# env = AQ_DRM_DEVICES,/dev/dri/card1:/dev/dri/card0 # Set explicit DRM devices
# env = AQ_MGPU_NO_EXPLICIT,1 # Disables explicit syncing on mgpu buffers
# env = AQ_NO_MODIFIERS,1 # Disables modifiers for DRM buffers

# =============================================================================
# HYPRLAND ENVIRONMENT VARIABLES
# =============================================================================
# https://wiki.hyprland.org/Configuring/Environment-variables/#hyprland-environment-variables
# Uncomment if needed for debugging:
# env = HYPRLAND_TRACE,1 # Enables more verbose logging
# env = HYPRLAND_NO_RT,1 # Disables realtime priority setting
# env = HYPRLAND_NO_SD_NOTIFY,1 # Disables sd_notify calls (systemd)
# env = HYPRLAND_NO_SD_VARS,1 # Disables systemd/dbus environment management

# =============================================================================
# NVIDIA CONFIGURATION
# =============================================================================
# For NVIDIA users, uncomment the line below to load NVIDIA-specific settings:
# source = $HOME/.config/hypr/UserConfigs/nvidia.conf.example
